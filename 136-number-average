public Decimal average(List<Decimal> numbers){
    Decimal total = 0;
    Integer numberOfItems = numbers.size();
    for (Decimal i : numbers){
        total += i;
    }
    Decimal average = total / numberOfItems;
    average = average.setScale(2);
    return average;
}