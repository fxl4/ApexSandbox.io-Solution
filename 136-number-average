public Decimal average(List<Decimal> numbers){
    Decimal total = 0;
    for (Decimal i : numberList){
        total += i;
    }
    Decimal average = total / numbers.size();
    average = average.round();
    return average;
}