public Boolean isPrime(Integer num) {
    Boolean isNumberPrime;
    Float numFloat = parseFloat(num);
    if (Math.mod(numFloat, numFloat) == 0) {
        isNumberPrime = true;
    } else {
        isNumberPrime = false;
    }
    return isNumberPrime;
}