public Boolean isPrime(Integer num) {
    Boolean isNumberPrime;
    Double numDouble = parseDouble(num);
    if (Math.mod(numDouble, numDouble) == 0) {
        isNumberPrime = true;
    } else {
        isNumberPrime = false;
    }
    return isNumberPrime;
}