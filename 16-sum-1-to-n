public Integer sumToN(Integer n) {
    Integer index = 1;
    Integer sum = 0;
    List<String> integerList;
    while (index != n) {
        index = index + 1;
        integerList.add(index);
    }
    if (index == n) {
        sum = integerList.sum();
        return sum;
    }
    
}