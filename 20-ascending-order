public Boolean ascendingOrder(Integer a, Integer b, Integer c) {
    if (a == b and b == c) {
        return true;
    } else if (a <= b and b <= c) {
        return true;
    } else {
        return false;
    }
}