public Integer diff(Integer a, Integer b) {
    if (a > b) {
        return Math.abs(a - b);
    } else if (a < b) {
        return Math.abs(b - a);
    } else {
        // a == b or one of them is 0
        return 0;
    }
}